import 'package:flutter/material.dart';

class MyVideoCard extends StatelessWidget {
  const MyVideoCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
