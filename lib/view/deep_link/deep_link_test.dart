import 'package:flash/main.dart';
import 'package:flutter/material.dart';

class DeepLinkTest extends StatelessWidget {
  const DeepLinkTest({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('deeplinktest'),
      ),
    );
  }
}
